{
  "wo": "WO-HELM-HERMES-PH2-RECOVERY-PLANNER-DIGEST-HARDENING-0001",
  "mode": "CODE_ONLY_HARDENING",
  "verdict": "GREEN_PR_OPEN_HERMES_PH2_RECOVERY_PLANNER_DIGEST_HARDENING_CODE_ONLY",
  "persona": "HELM", "lane": "HERMES",
  "base_sha": "115a19cd9490f0e3f279761f7cfdc1b65adb4fd3",
  "head_sha": "9049e362b7cfac6223ed3f7ef4d001f8a918694e",
  "pr": {"number": 96, "url": "https://github.com/maff0000/hermes/pull/96", "state": "OPEN"},
  "branch": "wo/WO-HELM-HERMES-PH2-RECOVERY-PLANNER-DIGEST-HARDENING-0001",
  "worktree": "/srv/trading/hermes-worktrees/wo-planner-digest-hardening",
  "changed_files": ["utils/hermes_recovery_planner_runtime_v1.py", "tests/test_hermes_recovery_planner_runtime_v1.py", "docs/design/recovery_planner_wiring/digest_hardening.md"],
  "recovery_relevant_digest": "tuple coverage element = _sha(recovery_relevant_scope): per timeframe with a retained gap -> {retained gap intervals (clipped to retention floor), coverage intersecting/adjacent within max(period, merge_threshold), authority=[provenance, contract_version]}. Pure planner still receives FULL coverage snapshots; output unchanged.",
  "excluded_volatile_fields": ["gaps generated_at/observed_at", "coverage snapshot/observation timestamp", "TTL", "publisher refresh metadata", "Redis retrieval / sorted-set order", "dict insertion order", "current-edge candles outside every relevant window", "sliding-window movement outside relevant gaps"],
  "included_material_fields": ["retained gap intervals", "gap-relevant coverage (intersecting/adjacent)", "coverage completeness within gaps", "retention classification of gaps", "coverage provenance/authority", "coverage contract_version", "semantic gaps digest", "closure digest", "policy digest", "planner version"],
  "gap_canonicalisation": "6/6 pass (reordered/duplicate/volatile stable; material interval + status detected; dict-order independent)",
  "coverage_canonicalisation": "7/7 pass (retrieval order + duplicate entries + beyond-retention/far exclusion; unrelated M1/M5/H1/sliding-window ignored)",
  "policy_pre_post": "5/5 pass (atomic replacement / removal / invalid replacement mid-cycle -> BLOCKED_INPUT_INCONSISTENCY; no stale fallback; unchanged passes; change reinvokes)",
  "coverage_pre_post": "4/4 pass (unrelated advance no retry; relevant advance bounded retry; authority change retry; perpetual -> BLOCKED_INPUT_INCONSISTENCY, bounded)",
  "snapshot_order": "gate -> policy(read+digest+identity) -> gaps A -> retained scope -> rel-coverage A -> closures -> gaps B -> policy recheck B -> rel-coverage B -> compare -> retry/block -> tuple -> hold/invoke",
  "cycle_clock": "3/3 pass (single clock sample per cycle; unrelated wall-clock stable; retention reclassification changes digest)",
  "five_cycle_idempotency": "planner_invocation_count==1, proposal_held_count==4, runner_cycle_count==5 (unrelated advances each cycle)",
  "material_change_matrix": "11/11 pass (relevant gap-fill / partial / completeness / retention reclass / provenance / contract-version / gap change / coverage change / policy change / closure change / planner-version change all reinvoke; volatile refresh holds)",
  "planner_invocation_accounting": "runner cycles distinct from planner invocations; 3 identical cycles -> runner_cycle_count=3, planner_invocation_count=1, proposal_held_count=2",
  "historical_cycle_count_correction": "accepted first-run PROPOSAL_READY invocations = THREE (HELM summary said four); evidence not rewritten",
  "gate_disabled_path": "unchanged: gate truth table pass; disabled path zero-read (pr.reads==0, fr.writes==[])",
  "frozen_decisions": "all 9 preserved; gaps key / canonical XAU_USD / XAUUSD rejected / D1 22:00 anchor / no stale-as-live / no config-in-code / no Redis writes / no SQL / no vendor / consumer_live=false all held",
  "mutation_boundary": "no Redis write/delete, no SQL, no disk write, no publication; only in-memory holder.set(); read-mode open() in _FilePolicyReader only",
  "legacy_separation": "imports = stdlib + env_config + hermes_logging + utils.hermes_recovery_planner_v1 (fully-qualified); NO utils.recovery_planner / recovery_executor / RecoveryLibrary",
  "new_tests": "41 new (runtime suite 42 -> 83, all pass)",
  "relevant_suite": "pure planner + runtime + publisher + gaps/backfill wiring: all pass",
  "base_vs_head": "BASE 1295 passed / HEAD 1336 passed (+41); 51 async failures + 4 collection errors identical both; zero regression",
  "performance": "1 planner invocation + >=4 held over 5 unrelated-advance cycles",
  "runtime_non_mutation": "deploy 115a19c; runners 9; gates unset; policy absent; no planner thread; no proposal/health/job key; PH1 D1 ACTIVE; PH2 gaps GAPS_FOUND + backfill READY_FOR_BACKFILL_DESIGN; quote RUNTIME_PUBLISHED; tick present; feed GREEN; consumer_live=false; host runtime blob 8aa7bd7e != hardened de4281b8 (not deployed)",
  "redis_namespace": "read-only GET/EXISTS/ZRANGE only; no write path",
  "cross_application": "no falcon/ares/helios/neo/solo/vendor/seed_backfill in executable code",
  "next_gate": "R2D2 cold audit of open PR #96"
}
