# NO-DEPLOY / NO-ACTIVATION / NO-REDIS-WRITE PROOF
- Code + tests only. No deploy, no container build, no restart, no env change, no activation.
- Default DISABLED: with D1 gates unset, build_h4_producer_from_env attaches a DisabledD1Producer -> the seal
  hook is a no-op. No flag set anywhere.
- No Redis I/O at import: the D1 producer/client is built only inside build_d1_producer_from_env on the enabled
  path. Tests use in-memory FakeRedis; zero live Redis connections by this WO.
- H4 latest + forward-history behaviour unchanged (offer runs after both; can never alter/block them).
