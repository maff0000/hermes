# NO-DEPLOY / NO-ACTIVATION / NO-REDIS-WRITE PROOF
- Code + tests only. No deploy, no container build, no restart, no env change, no activation.
- Default DISABLED: build_d1_producer_from_env -> DisabledD1Producer unless HERMES_CANDLE_D1_PUBLISH_ENABLED=true
  (with FORWARD_ENABLED + SINK=canonical). No flag set anywhere.
- No Redis I/O at import: the writer/client is built only inside build_d1_producer_from_env on the enabled path.
  Tests use in-memory FakeRedis; zero live Redis connections by this WO.
- The producer itself constructs no Redis client and does no .zadd; the canonical writer performs the SET only
  when activated. Latest behaviour of M1/M5/M15/H1/H4 and the forward-history writer is unchanged.
