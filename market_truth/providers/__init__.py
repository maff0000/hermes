"""market_truth.providers — concrete `MarketDataProvider` implementations.

HMT-1 implements exactly one: `fixture.FixtureMarketDataProvider`. No live provider adapter exists
in this package — that is out of scope for HMT-1 (see the module docstring on `market_truth.provider`).
"""
